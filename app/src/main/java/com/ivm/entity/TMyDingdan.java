package com.ivm.entity;import java.io.Serializable;import java.math.BigDecimal;import java.sql.Timestamp;/** * <br> * <b>功能：</b>TDingdanEntity<br> */public class TMyDingdan extends TDingdan {    public boolean isZhifu() {//        String stt = this.getDdState();//        if (stt.length() < 2)//            return false;//        if (stt.length() > 2)//            stt = stt.substring(stt.length() - 2);//        if (stt.charAt(0) > '0')//            return true;        return false;    }    public boolean isBeicheng() {//        String stt = this.getDdState();//        if (stt.length() < 3)//            return false;//        if (stt.length() > 3)//            stt = stt.substring(stt.length() - 3);//        if (stt.charAt(0) == '1')//            return true;        return false;    }    public boolean ischexiao() {//        String stt = this.getDdState();//        if (stt.length() < 4)//            return false;//        if (stt.length() > 4)//            stt = stt.substring(stt.length() - 4);//        if (stt.charAt(0) > '0')//            return true;        return false;    }    public boolean istihuo() {//        String stt = this.getDdState();//        if (stt.length() < 3)//            return false;//        if (stt.length() > 3)//            stt = stt.substring(stt.length() - 3);//        if (stt.charAt(0) == '5')//            return true;        return false;    }    public boolean isxinyongzhifu() {//        String stt = this.getDdState();//        if (stt.length() < 2)//            return false;//        if (stt.length() > 2)//            stt = stt.substring(stt.length() - 2);//        if (stt.equals("11"))//            return true;        return false;    }    public String ddstate() {        if (isZhifu()) {            if (isBeicheng())                return "已备成";            if (isxinyongzhifu())                return "信用支付";            return "已支付";        }        if (!isZhifu())            return "未支付";        return "";    }}